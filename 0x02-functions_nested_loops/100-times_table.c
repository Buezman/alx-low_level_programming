#include "main.h"
/**
 *
 * print_times_table - prints times table of a number
 *
 * @n: positive integer
 *
 * Return: 0 (success)
 *
 */
void print_times_table(int n)
{
	int i, j, x;

	if (n >=0 && n <= 15)
	{
		for (i = 0; i <=n; i++)
		{
			for (j = 0; j <= n; j++)
			{
				x = i * j;
				if (x < 
			}
		}
	}
}
